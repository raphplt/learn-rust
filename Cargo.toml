[package]
name = "rust-exercices"
version = "0.1.0"
edition = "2021"
description = "Apprendre Rust par la pratique — des bases jusqu'aux projets systèmes"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
